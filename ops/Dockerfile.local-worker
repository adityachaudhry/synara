FROM node:24-bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends git git-lfs bash tmux ca-certificates ripgrep python3 curl poppler-utils \
    && rm -rf /var/lib/apt/lists/* \
    && mkdir -p /workspace /opt/synara
WORKDIR /workspace
CMD ["sleep", "infinity"]
